<div class="max-w-7xl mx-auto py-10 px-4 sm:px-6 lg:px-8">
  <div class="mb-6">
    <%= link_to "← Back to Seasons", admin_seasons_path, class: "text-indigo-600 hover:text-indigo-900" %>
  </div>

  <% if notice.present? %>
    <div class="bg-green-50 border-l-4 border-green-400 p-4 mb-6">
      <div class="flex">
        <div class="flex-shrink-0">
          <svg class="h-5 w-5 text-green-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
          </svg>
        </div>
        <div class="ml-3">
          <p class="text-sm text-green-700"><%= notice %></p>
        </div>
      </div>
    </div>
  <% end %>

  <div class="bg-white shadow overflow-hidden sm:rounded-lg mb-6">
    <div class="flex justify-between items-center px-4 py-5 sm:px-6">
      <div>
        <h3 class="text-2xl font-bold leading-6 text-gray-900"><%= @season.name %></h3>
        <% if @season.active? %>
          <span class="mt-2 px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-green-100 text-green-800">Active</span>
        <% else %>
          <span class="mt-2 px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-gray-100 text-gray-800">Inactive</span>
        <% end %>
      </div>
      <div>
        <%= link_to "Edit", edit_admin_season_path(@season), class: "inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
      </div>
    </div>
    <div class="border-t border-gray-200">
      <dl>
        <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500">Description</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2"><%= @season.description.present? ? @season.description : "No description provided" %></dd>
        </div>
        <div class="bg-white px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500">Status</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2"><%= @season.active? ? "Active" : "Inactive" %></dd>
        </div>
        <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500">Started At</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2"><%= @season.started_at&.strftime("%B %d, %Y at %I:%M %p") || "Not started" %></dd>
        </div>
        <div class="bg-white px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500">Ended At</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2"><%= @season.ended_at&.strftime("%B %d, %Y at %I:%M %p") || "Ongoing" %></dd>
        </div>
        <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500">Transition Ends At</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2"><%= @season.transition_ends_at&.strftime("%B %d, %Y at %I:%M %p") || "Not set" %></dd>
        </div>
        <div class="bg-white px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500">Settings</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">
            <div class="whitespace-pre font-mono bg-gray-100 p-4 rounded-md overflow-auto max-h-60">
              <%= JSON.pretty_generate(@season.settings) rescue @season.settings.to_s %>
            </div>
          </dd>
        </div>
        <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500">Genre Trends</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">
            <div class="whitespace-pre font-mono bg-gray-100 p-4 rounded-md overflow-auto max-h-60">
              <%= JSON.pretty_generate(@season.genre_trends) rescue @season.genre_trends.to_s %>
            </div>
          </dd>
        </div>
        <div class="bg-white px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500">Created At</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2"><%= @season.created_at.strftime("%B %d, %Y at %I:%M %p") %></dd>
        </div>
        <div class="bg-gray-50 px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
          <dt class="text-sm font-medium text-gray-500">Updated At</dt>
          <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2"><%= @season.updated_at.strftime("%B %d, %Y at %I:%M %p") %></dd>
        </div>
      </dl>
    </div>
  </div>
  
  <div class="bg-white shadow overflow-hidden sm:rounded-lg mb-6">
    <div class="px-4 py-5 sm:px-6">
      <h3 class="text-lg leading-6 font-medium text-gray-900">Artist Generation</h3>
      <p class="mt-1 max-w-2xl text-sm text-gray-500">Generate AI artists for this season</p>
    </div>
    <div class="border-t border-gray-200 px-4 py-5 sm:p-6">
      <div class="mb-4">
        <p class="text-sm text-gray-700">
          <% if @season.settings.is_a?(Hash) && @season.settings.dig('artist_count').present? %>
            This season is configured to generate <%= @season.settings['artist_count'] %> artists when activated.
          <% else %>
            No artist count has been configured for this season. Edit the season to set the number of artists to generate.
          <% end %>
        </p>
      </div>
      
      <%= form_with url: generate_artists_admin_season_path(@season), method: :post, class: "mt-5" do |form| %>
        <div class="flex items-end space-x-4">
          <div>
            <%= form.label :count, "Number of artists to generate:", class: "block text-sm font-medium text-gray-700" %>
            <div class="mt-1">
              <%= form.number_field :count, min: 1, max: 1000, value: (@season.settings.is_a?(Hash) ? @season.settings.dig('artist_count') : nil) || 10, class: "shadow-sm focus:ring-indigo-500 focus:border-indigo-500 block w-full sm:text-sm border-gray-300 rounded-md" %>
            </div>
          </div>
          <%= form.submit "Generate Artists", class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
        </div>
        <p class="mt-2 text-xs text-gray-500">This will generate artists in the background. Check the logs for progress.</p>
      <% end %>
    </div>
  </div>
  
  <div class="bg-white shadow overflow-hidden sm:rounded-lg mb-6">
    <div class="px-4 py-5 sm:px-6">
      <h3 class="text-lg leading-6 font-medium text-gray-900">Venue Regeneration</h3>
      <p class="mt-1 max-w-2xl text-sm text-gray-500">Generate AI venues for this season</p>
    </div>
    <div class="border-t border-gray-200 px-4 py-5 sm:p-6">
      <div class="mb-4">
        <p class="text-sm text-gray-700">
          <% if @season.settings.is_a?(Hash) && @season.settings.dig('venue_count').present? %>
            This season is configured to generate <%= @season.settings['venue_count'] %> venues when activated.
          <% else %>
            Default venue count (25) will be used when activating this season. Edit the season to customize.
          <% end %>
        </p>
      </div>
      
      <%= form_with url: regenerate_venues_admin_season_path(@season), method: :post, class: "mt-5" do |form| %>
        <div class="flex items-end space-x-4">
          <div>
            <%= form.label :count, "Number of venues to generate:", class: "block text-sm font-medium text-gray-700" %>
            <div class="mt-1">
              <%= form.number_field :count, min: 1, max: 100, value: (@season.settings.is_a?(Hash) ? @season.settings.dig('venue_count') : nil) || 25, class: "shadow-sm focus:ring-indigo-500 focus:border-indigo-500 block w-full sm:text-sm border-gray-300 rounded-md" %>
            </div>
          </div>
          <%= form.submit "Regenerate Venues", class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
        </div>
        <p class="mt-2 text-xs text-gray-500">This will generate venues in the background. Only venues without performances will be replaced.</p>
      <% end %>
    </div>
  </div>
  
  <div class="flex justify-between">
    <%= link_to "Edit", edit_admin_season_path(@season), class: "inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
    <%= link_to "Delete Season", admin_season_path(@season), method: :delete, data: { confirm: "Are you sure you want to delete this season?" }, class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500" %>
  </div>
</div> 